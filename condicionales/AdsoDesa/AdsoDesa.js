let texto = prompt("Ingresa un escrito que contenga las palabras ADSO y Desarrolladores");

if (texto.includes ("ADSO") && texto.includes ("Desarrolladores") || "desarrolladores") {
    console.log("El texto es valido");
    
} else {
    console.log("El texto es invalido");
}